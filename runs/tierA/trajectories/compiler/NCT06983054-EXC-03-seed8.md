### 1. instructions (prompt compiler-v3)

```
You prepare clinical trial eligibility criteria for automated checking against
structured medical records.

You are careful about what a medical record can and cannot settle. Most criteria
in a real protocol cannot be settled from a record at all, and saying so is the
correct answer rather than a failure.

Decide whether this criterion can be checked from a structured medical record,
and list the clinical concepts it needs.

Criterion ({kind}, category {category}):
  {text}

A criterion is checkable ONLY if every part of it can be settled from coded
record data: demographics, coded diagnoses, coded medications, coded procedures,
or numeric laboratory and vital-sign values, with dates.

The record carries an index date: the day this prescreen is being run. Study
events that have not happened yet are anchored to it. "at screening", "at
randomisation", "at the time of signing the informed consent form", "at baseline",
"prior to randomisation" all mean "as of the index date", and a window measured
back from one of them is measured back from the index date. That substitution is
what prescreening is, so it does not make a criterion uncheckable. Say in `reason`
that you used it, so the reviewer sees the assumption.

What stays uncheckable is the participant, not the calendar: whether they will
consent, whether they are able to comply, what the investigator will judge.

It is NOT checkable if it needs any of:
  - consent, willingness, capacity, ability to comply or to attend
  - the investigator's opinion, or a judgement about suitability
  - a measurement a record does not normally hold (waist circumference,
    performance status, imaging read-outs, questionnaire scores, elastography)
  - counting prior lines of therapy, or a washout expressed in half-lives
  - a bound relative to a laboratory's own reference range, such as "2.5x the
    upper limit of normal", since the limit is not a fixed number
  - contraception, pregnancy intention, or residence and travel plans
  - enrolment in another study

Return JSON only:

{{"checkable": true,
  "reason": "one sentence; if not checkable, name the specific blocker",
  "concepts": [{{"name":"SGLT2 inhibitor","domain":"medication",
                 "intent":"currently prescribed drug of this class"}}]}}

`concepts` lists every clinical thing that must be looked up by code. Leave it
empty when the criterion uses only age or sex. Use the exact domain the record
would store it in.

---

You translate one clinical trial eligibility criterion into a formal predicate.

You are precise and literal. You express exactly what the criterion says, and you
use only the codes you are given.

Translate this criterion into a predicate.

Criterion ({kind}, category {category}):
  {text}

PREDICATE GRAMMAR

expr:
  {"op":"and","args":[expr,...]}
  {"op":"or","args":[expr,...]}
  {"op":"not","arg":expr}
  {"op":"at_least","n":INT,"args":[expr,...]}        for "at least N of the following"
  {"op":"compare","cmp":">|>=|<|<=|==|!=","left":value,"right":value}
  {"op":"between","value":value,"low":NUM,"high":NUM,"inclusive":[BOOL,BOOL]}
  {"op":"exists","query":query}
  {"op":"const","value":"TRUE|FALSE|UNKNOWN"}

value:
  {"val":"age"}                                       age in years at the screening date
  {"val":"sex"}
  {"val":"literal","number":NUM,"unit":"STR"}
  {"val":"observation","codes":["LOINC",...],"unit":"STR","agg":"latest|min|max|first",
   "within_days":INT|null}
  {"val":"derived","name":"egfr_ckdepi_2021|bmi|systolic_bp|diastolic_bp","within_days":INT|null}
  {"val":"count","query":query}

query:
  {"domain":"condition|medication|procedure|observation","codes":["CODE",...],
   "broader_codes":["CODE",...],
   "within_days":INT|null,"active_only":BOOL,"absent_means":"false|unknown"}

RULES

* `unit` on an observation is the unit the CRITERION is written in, not the unit
  the record stores. Conversion is handled for you. State the criterion's unit.
* `within_days` encodes a temporal window. "within the past 3 months" is 91.
  "within 6 months" is 183. "within 1 month" is 30. null means no window.
* `absent_means` decides what an empty result means, and it is the most
  consequential field here.
    "false"   - this record can be trusted to be complete for these codes, so
                nothing found means the patient does not have it. Reasonable for
                a reconciled medication list or a coded problem list.
    "unknown" - silence proves nothing. Correct for anything that may have
                happened at another site, for events like a hospitalisation, and
                for anything acute or recent.
  When in doubt choose "unknown". A wrong "false" rules a patient out of a trial
  on the strength of a gap in their record, and nobody ever audits that.
* `broader_codes` are codes this site uses that CONTAIN the concept without
  establishing it, listed for you in the GROUNDED CONCEPTS block. Copy them into
  the query when they are given. They behave differently from `codes` on purpose:
  a broader code found in the record makes the answer undetermined rather than
  true, while finding nothing at all still means whatever `absent_means` says.
  That is not a hedge, it is the actual state of knowledge. A site that records
  one unspecified diabetes code cannot tell you a patient has type 2, and can
  still tell you a patient has no diabetes of any kind.
* Never invent a code. Use only codes from the GROUNDED CONCEPTS block.
* An exclusion criterion is expressed as the thing that would EXCLUDE the
  patient. `exists(current SGLT2 inhibitor)` is a correct exclusion predicate.
* Model the criterion as written. Do not add a clinical safety margin, do not
  widen a range, and do not add a condition the text does not state.

GROUNDED CONCEPTS
=================
{grounded}

WORKED EXAMPLES

Criterion (inclusion): "HbA1c 6.5-10%"
  Grounded: HbA1c (observation) codes ["4548-4"]
  {"expr": {"op":"between",
            "value":{"val":"observation","codes":["4548-4"],"unit":"%",
                     "agg":"latest","within_days":null},
            "low":6.5,"high":10.0,"inclusive":[true,true]}}

Criterion (inclusion): "Age 18 - 85 years of age"
  Grounded: none
  {"expr": {"op":"between","value":{"val":"age"},"low":18,"high":85,
            "inclusive":[true,true]}}

Criterion (inclusion): "BMI greater than or equal to 27.0 kg/m^2"
  Grounded: Body Mass Index (observation) codes ["39156-5"]
  {"expr": {"op":"compare","cmp":">=",
            "left":{"val":"derived","name":"bmi","within_days":null},
            "right":{"val":"literal","number":27.0,"unit":"kg/m2"}}}

Criterion (exclusion): "Myocardial infarction or stroke within 180 days before screening"
  Grounded: myocardial infarction (condition) ["22298006","399211009"],
            stroke (condition) ["230690007"]
  {"expr": {"op":"or","args":[
      {"op":"exists","query":{"domain":"condition","codes":["22298006","399211009"],
                              "within_days":180,"absent_means":"unknown"}},
      {"op":"exists","query":{"domain":"condition","codes":["230690007"],
                              "within_days":180,"absent_means":"unknown"}}]}}
  absent_means is "unknown" because a myocardial infarction may have been treated
  at another hospital, and silence must not clear the patient.

Criterion (inclusion): "at least one of: blood pressure >=130 systolic, or
                        triglycerides >=150 mg/dL"
  {"expr": {"op":"at_least","n":1,"args":[
      {"op":"compare","cmp":">=","left":{"val":"derived","name":"systolic_bp",
                                          "within_days":null},
       "right":{"val":"literal","number":130,"unit":"mmHg"}},
      {"op":"compare","cmp":">=","left":{"val":"observation","codes":["2571-8"],
                                          "unit":"mg/dL","agg":"latest","within_days":null},
       "right":{"val":"literal","number":150,"unit":"mg/dL"}}]}}

Return JSON only:

{{"expr": <expr>,
  "unit_note": "which units the criterion is written in",
  "absence_note": "why each absent_means was chosen",
  "confidence": "high|medium|low"}}
```

### 2. input

```json
{
 "category": "medication",
 "criterion_id": "NCT06983054-EXC-03",
 "kind": "exclusion",
 "source_text": "Current/chronic use of the following medication: SGLT2 inhibitors, TZD, GLP-1RA, glucocorticoids, immune suppressants, antimicrobial agents, chemotherapeutics"
}
```

### 3. llm_request -> gemini-3.7-flash-medium  cassette `10e9ae290bd43933`

```
[system]
You prepare clinical trial eligibility criteria for automated checking against
structured medical records.

You are careful about what a medical record can and cannot settle. Most criteria
in a real protocol cannot be settled from a record at all, and saying so is the
correct answer rather than a failure.

[user]
Decide whether this criterion can be checked from a structured medical record,
and list the clinical concepts it needs.

Criterion (exclusion, category medication):
  Current/chronic use of the following medication: SGLT2 inhibitors, TZD, GLP-1RA, glucocorticoids, immune suppressants, antimicrobial agents, chemotherapeutics

A criterion is checkable ONLY if every part of it can be settled from coded
record data: demographics, coded diagnoses, coded medications, coded procedures,
or numeric laboratory and vital-sign values, with dates.

The record carries an index date: the day this prescreen is being run. Study
events that have not happened yet are anchored to it. "at screening", "at
randomisation", "at the time of signing the informed consent form", "at baseline",
"prior to randomisation" all mean "as of the index date", and a window measured
back from one of them is measured back from the index date. That substitution is
what prescreening is, so it does not make a criterion uncheckable. Say in `reason`
that you used it, so the reviewer sees the assumption.

What stays uncheckable is the participant, not the calendar: whether they will
consent, whether they are able to comply, what the investigator will judge.

It is NOT checkable if it needs any of:
  - consent, willingness, capacity, ability to comply or to attend
  - the investigator's opinion, or a judgement about suitability
  - a measurement a record does not normally hold (waist circumference,
    performance status, imaging read-outs, questionnaire scores, elastography)
  - counting prior lines of therapy, or a washout expressed in half-lives
  - a bound relative to a laboratory's own reference range, such as "2.5x the
    upper limit of normal", since the limit is not a fixed number
  - contraception, pregnancy intention, or residence and travel plans
  - enrolment in another study

Return JSON only:

{"checkable": true,
  "reason": "one sentence; if not checkable, name the specific blocker",
  "concepts": [{"name":"SGLT2 inhibitor","domain":"medication",
                 "intent":"currently prescribed drug of this class"}]}

`concepts` lists every clinical thing that must be looked up by code. Leave it
empty when the criterion uses only age or sex. Use the exact domain the record
would store it in.
```

### 4. llm_response (openai, 309 tok, 29.1826171875s)

```
{
  "checkable": true,
  "reason": "Current or chronic medication use can be evaluated from active coded medication orders or dispensations anchored to the index date.",
  "concepts": [
    {
      "name": "SGLT2 inhibitor",
      "domain": "medication",
      "intent": "currently prescribed or active drug of this class"
    },
    {
      "name": "Thiazolidinedione (TZD)",
      "domain": "medication",
      "intent": "currently prescribed or active drug of this class"
    },
    {
      "name": "GLP-1 receptor agonist (GLP-1RA)",
      "domain": "medication",
      "intent": "currently prescribed or active drug of this class"
    },
    {
      "name": "Glucocorticoid",
      "domain": "medication",
      "intent": "currently prescribed or active drug of this class"
    },
    {
      "name": "Immunosuppressant",
      "domain": "medication",
      "intent": "currently prescribed or active drug of this class"
    },
    {
      "name": "Antimicrobial agent",
      "domain": "medication",
      "intent": "currently prescribed or active drug of this class"
    },
    {
      "name": "Chemotherapeutic agent",
      "domain": "medication",
      "intent": "currently prescribed or active drug of this class"
    }
  ]
}
```

### 5. instructions (prompt grounder-v3)

```
You are a clinical terminologist. You expand a clinical concept into the
specific named things a medical record would actually store for it.

Expand this clinical concept into concrete searchable names.

Concept: {concept}
Record domain: {domain}

For a drug class, list the generic ingredient names that belong to the class.
For a diagnosis, list the specific condition names, including the common
sub-types a record might code instead of the general term.
For a laboratory test, list the names and common synonyms of the measurement.

List what belongs to the concept in general clinical practice. Do NOT try to
guess what this particular site happens to record; that is looked up separately.

Return JSON only:
{{"names": ["empagliflozin", "dapagliflozin"], "note": "one short line"}}

Give between 1 and 15 names.

---

You match a clinical concept against a specific site's coded vocabulary.

You choose only from the candidates given. You never invent a code. If none of
the candidates genuinely represents the concept, you say so plainly, because a
wrong match here is worse than no match.

Concept: {concept}
Record domain: {domain}
Meaning required: {intent}

Candidate entries from this site's vocabulary:
{candidates}

Select every candidate that genuinely represents the concept. Judge by what the
entry means, not by whether the words look similar. A different measurement that
shares a word is not a match: "Respiratory rate" is not a glomerular filtration
rate, and "Chronic sinusitis" is not chronic kidney disease.

The display text beside each code is whatever the source system chose to store.
It is frequently abbreviated, locally worded, or vaguer than the code it labels,
and it is not the code's definition. Judge the concept the code denotes. Where you
recognise a code and its display disagree about how specific the concept is, the
code decides, and name the code you relied on in `why` for the ones you rejected.
This does not license guessing: a code you do not recognise is judged on its
display like any other candidate, and a display that is vaguer than the concept is
still a reason to reject a code you have no independent knowledge of.

For a drug class, select the entries whose ingredient belongs to that class.
Different strengths or formulations of the same ingredient are all matches.

A candidate can also be BROADER than the concept: it contains what the criterion
asks for and other things as well. A site that records every diabetes diagnosis
under one unspecified diabetes code has a code that is broader than "type 2
diabetes mellitus". A site whose only asthma code is "chronic lower respiratory
disease" has one that is broader than asthma.

Put those in `broader_codes`, not in `codes`. The difference decides what happens
to a patient. A code in `codes` proves the concept when it is present. A code in
`broader_codes` cannot prove it, and the criterion comes back undetermined for
that patient rather than satisfied. Its absence still counts: a patient with no
diabetes code of any kind does not have type 2 diabetes either.

Do not use `broader_codes` for a code that is merely related, or for a
complication of the concept, or for a different thing that shares a word. Only
for a code whose meaning genuinely contains the concept.

If no candidate represents the concept and none contains it, return both lists
empty. That is a normal and useful answer.

Return JSON only:
{{"codes": ["4548-4"], "broader_codes": [],
  "rejected": [{{"code": "718-7", "why": "different analyte"}}],
  "confidence": "high"}}
```

### 6. input

```json
{
 "concept": "SGLT2 inhibitor",
 "domain": "medication",
 "intent": "currently prescribed or active drug of this class"
}
```

### 7. llm_request -> gemini-3.7-flash-medium  cassette `57984fc64c042486`

```
[system]
You are a clinical terminologist. You expand a clinical concept into the
specific named things a medical record would actually store for it.

[user]
Expand this clinical concept into concrete searchable names.

Concept: SGLT2 inhibitor
Record domain: medication

For a drug class, list the generic ingredient names that belong to the class.
For a diagnosis, list the specific condition names, including the common
sub-types a record might code instead of the general term.
For a laboratory test, list the names and common synonyms of the measurement.

List what belongs to the concept in general clinical practice. Do NOT try to
guess what this particular site happens to record; that is looked up separately.

Return JSON only:
{"names": ["empagliflozin", "dapagliflozin"], "note": "one short line"}

Give between 1 and 15 names.
```

### 8. llm_response (openai, 86 tok, 32.63436818122864s)

```
```json
{
  "names": [
    "empagliflozin",
    "dapagliflozin",
    "canagliflozin",
    "ertugliflozin",
    "bexagliflozin",
    "sotagliflozin",
    "ipragliflozin",
    "luseogliflozin",
    "tofogliflozin",
    "remogliflozin"
  ],
  "note": "Generic active ingredient names for sodium-glucose cotransporter-2 (SGLT2) inhibitors."
}
```
```

### 9. tool_call `terminology.search_any`

```json
{
 "domain": "medication",
 "limit": 12,
 "terms": [
  "empagliflozin",
  "dapagliflozin",
  "canagliflozin",
  "ertugliflozin",
  "bexagliflozin",
  "sotagliflozin",
  "ipragliflozin",
  "luseogliflozin",
  "tofogliflozin",
  "remogliflozin"
 ]
}
```

### 10. tool_result `terminology.search_any`

```json
[]
```

### 11. final

```json
{
 "broader_codes": [],
 "codes": [],
 "concept": "SGLT2 inhibitor",
 "confidence": "high",
 "domain": "medication",
 "expanded_names": [
  "empagliflozin",
  "dapagliflozin",
  "canagliflozin",
  "ertugliflozin",
  "bexagliflozin",
  "sotagliflozin",
  "ipragliflozin",
  "luseogliflozin",
  "tofogliflozin",
  "remogliflozin"
 ],
 "reason": "no entry in this site's medication vocabulary matches any of empagliflozin, dapagliflozin, canagliflozin, ertugliflozin, bexagliflozin, sotagliflozin and others",
 "status": "UNMAPPABLE"
}
```

### 12. instructions (prompt grounder-v3)

```
You are a clinical terminologist. You expand a clinical concept into the
specific named things a medical record would actually store for it.

Expand this clinical concept into concrete searchable names.

Concept: {concept}
Record domain: {domain}

For a drug class, list the generic ingredient names that belong to the class.
For a diagnosis, list the specific condition names, including the common
sub-types a record might code instead of the general term.
For a laboratory test, list the names and common synonyms of the measurement.

List what belongs to the concept in general clinical practice. Do NOT try to
guess what this particular site happens to record; that is looked up separately.

Return JSON only:
{{"names": ["empagliflozin", "dapagliflozin"], "note": "one short line"}}

Give between 1 and 15 names.

---

You match a clinical concept against a specific site's coded vocabulary.

You choose only from the candidates given. You never invent a code. If none of
the candidates genuinely represents the concept, you say so plainly, because a
wrong match here is worse than no match.

Concept: {concept}
Record domain: {domain}
Meaning required: {intent}

Candidate entries from this site's vocabulary:
{candidates}

Select every candidate that genuinely represents the concept. Judge by what the
entry means, not by whether the words look similar. A different measurement that
shares a word is not a match: "Respiratory rate" is not a glomerular filtration
rate, and "Chronic sinusitis" is not chronic kidney disease.

The display text beside each code is whatever the source system chose to store.
It is frequently abbreviated, locally worded, or vaguer than the code it labels,
and it is not the code's definition. Judge the concept the code denotes. Where you
recognise a code and its display disagree about how specific the concept is, the
code decides, and name the code you relied on in `why` for the ones you rejected.
This does not license guessing: a code you do not recognise is judged on its
display like any other candidate, and a display that is vaguer than the concept is
still a reason to reject a code you have no independent knowledge of.

For a drug class, select the entries whose ingredient belongs to that class.
Different strengths or formulations of the same ingredient are all matches.

A candidate can also be BROADER than the concept: it contains what the criterion
asks for and other things as well. A site that records every diabetes diagnosis
under one unspecified diabetes code has a code that is broader than "type 2
diabetes mellitus". A site whose only asthma code is "chronic lower respiratory
disease" has one that is broader than asthma.

Put those in `broader_codes`, not in `codes`. The difference decides what happens
to a patient. A code in `codes` proves the concept when it is present. A code in
`broader_codes` cannot prove it, and the criterion comes back undetermined for
that patient rather than satisfied. Its absence still counts: a patient with no
diabetes code of any kind does not have type 2 diabetes either.

Do not use `broader_codes` for a code that is merely related, or for a
complication of the concept, or for a different thing that shares a word. Only
for a code whose meaning genuinely contains the concept.

If no candidate represents the concept and none contains it, return both lists
empty. That is a normal and useful answer.

Return JSON only:
{{"codes": ["4548-4"], "broader_codes": [],
  "rejected": [{{"code": "718-7", "why": "different analyte"}}],
  "confidence": "high"}}
```

### 13. input

```json
{
 "concept": "Thiazolidinedione (TZD)",
 "domain": "medication",
 "intent": "currently prescribed or active drug of this class"
}
```

### 14. llm_request -> gemini-3.7-flash-medium  cassette `a2ea8c00c6eabca2`

```
[system]
You are a clinical terminologist. You expand a clinical concept into the
specific named things a medical record would actually store for it.

[user]
Expand this clinical concept into concrete searchable names.

Concept: Thiazolidinedione (TZD)
Record domain: medication

For a drug class, list the generic ingredient names that belong to the class.
For a diagnosis, list the specific condition names, including the common
sub-types a record might code instead of the general term.
For a laboratory test, list the names and common synonyms of the measurement.

List what belongs to the concept in general clinical practice. Do NOT try to
guess what this particular site happens to record; that is looked up separately.

Return JSON only:
{"names": ["empagliflozin", "dapagliflozin"], "note": "one short line"}

Give between 1 and 15 names.
```

### 15. llm_response (openai, 80 tok, 29.554048776626587s)

```
```json
{
  "names": [
    "pioglitazone",
    "rosiglitazone",
    "troglitazone",
    "lobeglitazone",
    "balaglitazone",
    "ciglitazone",
    "darglitazone",
    "englitazone",
    "rivoglitazone"
  ],
  "note": "Generic ingredient names for the thiazolidinedione (glitazone) class of antidiabetic agents."
}
```
```

### 16. tool_call `terminology.search_any`

```json
{
 "domain": "medication",
 "limit": 12,
 "terms": [
  "pioglitazone",
  "rosiglitazone",
  "troglitazone",
  "lobeglitazone",
  "balaglitazone",
  "ciglitazone",
  "darglitazone",
  "englitazone",
  "rivoglitazone"
 ]
}
```

### 17. tool_result `terminology.search_any`

```json
[]
```

### 18. final

```json
{
 "broader_codes": [],
 "codes": [],
 "concept": "Thiazolidinedione (TZD)",
 "confidence": "high",
 "domain": "medication",
 "expanded_names": [
  "pioglitazone",
  "rosiglitazone",
  "troglitazone",
  "lobeglitazone",
  "balaglitazone",
  "ciglitazone",
  "darglitazone",
  "englitazone",
  "rivoglitazone"
 ],
 "reason": "no entry in this site's medication vocabulary matches any of pioglitazone, rosiglitazone, troglitazone, lobeglitazone, balaglitazone, ciglitazone and others",
 "status": "UNMAPPABLE"
}
```

### 19. instructions (prompt grounder-v3)

```
You are a clinical terminologist. You expand a clinical concept into the
specific named things a medical record would actually store for it.

Expand this clinical concept into concrete searchable names.

Concept: {concept}
Record domain: {domain}

For a drug class, list the generic ingredient names that belong to the class.
For a diagnosis, list the specific condition names, including the common
sub-types a record might code instead of the general term.
For a laboratory test, list the names and common synonyms of the measurement.

List what belongs to the concept in general clinical practice. Do NOT try to
guess what this particular site happens to record; that is looked up separately.

Return JSON only:
{{"names": ["empagliflozin", "dapagliflozin"], "note": "one short line"}}

Give between 1 and 15 names.

---

You match a clinical concept against a specific site's coded vocabulary.

You choose only from the candidates given. You never invent a code. If none of
the candidates genuinely represents the concept, you say so plainly, because a
wrong match here is worse than no match.

Concept: {concept}
Record domain: {domain}
Meaning required: {intent}

Candidate entries from this site's vocabulary:
{candidates}

Select every candidate that genuinely represents the concept. Judge by what the
entry means, not by whether the words look similar. A different measurement that
shares a word is not a match: "Respiratory rate" is not a glomerular filtration
rate, and "Chronic sinusitis" is not chronic kidney disease.

The display text beside each code is whatever the source system chose to store.
It is frequently abbreviated, locally worded, or vaguer than the code it labels,
and it is not the code's definition. Judge the concept the code denotes. Where you
recognise a code and its display disagree about how specific the concept is, the
code decides, and name the code you relied on in `why` for the ones you rejected.
This does not license guessing: a code you do not recognise is judged on its
display like any other candidate, and a display that is vaguer than the concept is
still a reason to reject a code you have no independent knowledge of.

For a drug class, select the entries whose ingredient belongs to that class.
Different strengths or formulations of the same ingredient are all matches.

A candidate can also be BROADER than the concept: it contains what the criterion
asks for and other things as well. A site that records every diabetes diagnosis
under one unspecified diabetes code has a code that is broader than "type 2
diabetes mellitus". A site whose only asthma code is "chronic lower respiratory
disease" has one that is broader than asthma.

Put those in `broader_codes`, not in `codes`. The difference decides what happens
to a patient. A code in `codes` proves the concept when it is present. A code in
`broader_codes` cannot prove it, and the criterion comes back undetermined for
that patient rather than satisfied. Its absence still counts: a patient with no
diabetes code of any kind does not have type 2 diabetes either.

Do not use `broader_codes` for a code that is merely related, or for a
complication of the concept, or for a different thing that shares a word. Only
for a code whose meaning genuinely contains the concept.

If no candidate represents the concept and none contains it, return both lists
empty. That is a normal and useful answer.

Return JSON only:
{{"codes": ["4548-4"], "broader_codes": [],
  "rejected": [{{"code": "718-7", "why": "different analyte"}}],
  "confidence": "high"}}
```

### 20. input

```json
{
 "concept": "GLP-1 receptor agonist (GLP-1RA)",
 "domain": "medication",
 "intent": "currently prescribed or active drug of this class"
}
```

### 21. llm_request -> gemini-3.7-flash-medium  cassette `1fe98ead1339fbc8`

```
[system]
You are a clinical terminologist. You expand a clinical concept into the
specific named things a medical record would actually store for it.

[user]
Expand this clinical concept into concrete searchable names.

Concept: GLP-1 receptor agonist (GLP-1RA)
Record domain: medication

For a drug class, list the generic ingredient names that belong to the class.
For a diagnosis, list the specific condition names, including the common
sub-types a record might code instead of the general term.
For a laboratory test, list the names and common synonyms of the measurement.

List what belongs to the concept in general clinical practice. Do NOT try to
guess what this particular site happens to record; that is looked up separately.

Return JSON only:
{"names": ["empagliflozin", "dapagliflozin"], "note": "one short line"}

Give between 1 and 15 names.
```

### 22. llm_response (openai, 67 tok, 30.409494876861572s)

```
```json
{
  "names": [
    "semaglutide",
    "liraglutide",
    "dulaglutide",
    "exenatide",
    "lixisenatide",
    "tirzepatide",
    "albiglutide"
  ],
  "note": "Generic ingredient names for GLP-1 receptor agonists and dual GIP/GLP-1 receptor agonists"
}
```
```

### 23. tool_call `terminology.search_any`

```json
{
 "domain": "medication",
 "limit": 12,
 "terms": [
  "semaglutide",
  "liraglutide",
  "dulaglutide",
  "exenatide",
  "lixisenatide",
  "tirzepatide",
  "albiglutide"
 ]
}
```

### 24. tool_result `terminology.search_any`

```json
[]
```

### 25. final

```json
{
 "broader_codes": [],
 "codes": [],
 "concept": "GLP-1 receptor agonist (GLP-1RA)",
 "confidence": "high",
 "domain": "medication",
 "expanded_names": [
  "semaglutide",
  "liraglutide",
  "dulaglutide",
  "exenatide",
  "lixisenatide",
  "tirzepatide",
  "albiglutide"
 ],
 "reason": "no entry in this site's medication vocabulary matches any of semaglutide, liraglutide, dulaglutide, exenatide, lixisenatide, tirzepatide and others",
 "status": "UNMAPPABLE"
}
```

### 26. instructions (prompt grounder-v3)

```
You are a clinical terminologist. You expand a clinical concept into the
specific named things a medical record would actually store for it.

Expand this clinical concept into concrete searchable names.

Concept: {concept}
Record domain: {domain}

For a drug class, list the generic ingredient names that belong to the class.
For a diagnosis, list the specific condition names, including the common
sub-types a record might code instead of the general term.
For a laboratory test, list the names and common synonyms of the measurement.

List what belongs to the concept in general clinical practice. Do NOT try to
guess what this particular site happens to record; that is looked up separately.

Return JSON only:
{{"names": ["empagliflozin", "dapagliflozin"], "note": "one short line"}}

Give between 1 and 15 names.

---

You match a clinical concept against a specific site's coded vocabulary.

You choose only from the candidates given. You never invent a code. If none of
the candidates genuinely represents the concept, you say so plainly, because a
wrong match here is worse than no match.

Concept: {concept}
Record domain: {domain}
Meaning required: {intent}

Candidate entries from this site's vocabulary:
{candidates}

Select every candidate that genuinely represents the concept. Judge by what the
entry means, not by whether the words look similar. A different measurement that
shares a word is not a match: "Respiratory rate" is not a glomerular filtration
rate, and "Chronic sinusitis" is not chronic kidney disease.

The display text beside each code is whatever the source system chose to store.
It is frequently abbreviated, locally worded, or vaguer than the code it labels,
and it is not the code's definition. Judge the concept the code denotes. Where you
recognise a code and its display disagree about how specific the concept is, the
code decides, and name the code you relied on in `why` for the ones you rejected.
This does not license guessing: a code you do not recognise is judged on its
display like any other candidate, and a display that is vaguer than the concept is
still a reason to reject a code you have no independent knowledge of.

For a drug class, select the entries whose ingredient belongs to that class.
Different strengths or formulations of the same ingredient are all matches.

A candidate can also be BROADER than the concept: it contains what the criterion
asks for and other things as well. A site that records every diabetes diagnosis
under one unspecified diabetes code has a code that is broader than "type 2
diabetes mellitus". A site whose only asthma code is "chronic lower respiratory
disease" has one that is broader than asthma.

Put those in `broader_codes`, not in `codes`. The difference decides what happens
to a patient. A code in `codes` proves the concept when it is present. A code in
`broader_codes` cannot prove it, and the criterion comes back undetermined for
that patient rather than satisfied. Its absence still counts: a patient with no
diabetes code of any kind does not have type 2 diabetes either.

Do not use `broader_codes` for a code that is merely related, or for a
complication of the concept, or for a different thing that shares a word. Only
for a code whose meaning genuinely contains the concept.

If no candidate represents the concept and none contains it, return both lists
empty. That is a normal and useful answer.

Return JSON only:
{{"codes": ["4548-4"], "broader_codes": [],
  "rejected": [{{"code": "718-7", "why": "different analyte"}}],
  "confidence": "high"}}
```

### 27. input

```json
{
 "concept": "Glucocorticoid",
 "domain": "medication",
 "intent": "currently prescribed or active drug of this class"
}
```

### 28. llm_request -> gemini-3.7-flash-medium  cassette `9e8d794250f980c7`

```
[system]
You are a clinical terminologist. You expand a clinical concept into the
specific named things a medical record would actually store for it.

[user]
Expand this clinical concept into concrete searchable names.

Concept: Glucocorticoid
Record domain: medication

For a drug class, list the generic ingredient names that belong to the class.
For a diagnosis, list the specific condition names, including the common
sub-types a record might code instead of the general term.
For a laboratory test, list the names and common synonyms of the measurement.

List what belongs to the concept in general clinical practice. Do NOT try to
guess what this particular site happens to record; that is looked up separately.

Return JSON only:
{"names": ["empagliflozin", "dapagliflozin"], "note": "one short line"}

Give between 1 and 15 names.
```

### 29. llm_response (openai, 99 tok, 42.290907859802246s)

```
```json
{
  "names": [
    "prednisone",
    "prednisolone",
    "methylprednisolone",
    "dexamethasone",
    "hydrocortisone",
    "triamcinolone",
    "betamethasone",
    "budesonide",
    "fluticasone",
    "beclomethasone",
    "mometasone",
    "cortisone",
    "deflazacort",
    "clobetasol"
  ],
  "note": "Common systemic and topical glucocorticoid generic active ingredients."
}
```
```

### 30. tool_call `terminology.search_any`

```json
{
 "domain": "medication",
 "limit": 12,
 "terms": [
  "prednisone",
  "prednisolone",
  "methylprednisolone",
  "dexamethasone",
  "hydrocortisone",
  "triamcinolone",
  "betamethasone",
  "budesonide",
  "fluticasone",
  "beclomethasone",
  "mometasone",
  "cortisone",
  "deflazacort",
  "clobetasol"
 ]
}
```

### 31. tool_result `terminology.search_any`

```json
[
 {
  "code": "312617",
  "display": "predniSONE 5 MG Oral Tablet",
  "domain": "medication",
  "n_resources": 6
 },
 {
  "code": "312615",
  "display": "predniSONE 20 MG Oral Tablet",
  "domain": "medication",
  "n_resources": 1
 },
 {
  "code": "106258",
  "display": "Hydrocortisone 10 MG/ML Topical Cream",
  "domain": "medication",
  "n_resources": 20
 },
 {
  "code": "895994",
  "display": "120 ACTUAT Fluticasone propionate 0.044 MG/ACTUAT Metered Dose Inhaler",
  "domain": "medication",
  "n_resources": 1199
 },
 {
  "code": "896209",
  "display": "60 ACTUAT Fluticasone propionate 0.25 MG/ACTUAT / salmeterol 0.05 MG/ACTUAT Dry Powder Inhaler",
  "domain": "medication",
  "n_resources": 187
 }
]
```

### 32. llm_request -> gemini-3.7-flash-medium  cassette `f31bdc5851aec16d`

```
[system]
You match a clinical concept against a specific site's coded vocabulary.

You choose only from the candidates given. You never invent a code. If none of
the candidates genuinely represents the concept, you say so plainly, because a
wrong match here is worse than no match.

[user]
Concept: Glucocorticoid
Record domain: medication
Meaning required: currently prescribed or active drug of this class

Candidate entries from this site's vocabulary:
            312617  predniSONE 5 MG Oral Tablet   (in 6 resources)
            312615  predniSONE 20 MG Oral Tablet   (in 1 resources)
            106258  Hydrocortisone 10 MG/ML Topical Cream   (in 20 resources)
            895994  120 ACTUAT Fluticasone propionate 0.044 MG/ACTUAT Metered Dose Inhaler   (in 1199 resources)
            896209  60 ACTUAT Fluticasone propionate 0.25 MG/ACTUAT / salmeterol 0.05 MG/A   (in 187 resources)

Select every candidate that genuinely represents the concept. Judge by what the
entry means, not by whether the words look similar. A different measurement that
shares a word is not a match: "Respiratory rate" is not a glomerular filtration
rate, and "Chronic sinusitis" is not chronic kidney disease.

The display text beside each code is whatever the source system chose to store.
It is frequently abbreviated, locally worded, or vaguer than the code it labels,
and it is not the code's definition. Judge the concept the code denotes. Where you
recognise a code and its display disagree about how specific the concept is, the
code decides, and name the code you relied on in `why` for the ones you rejected.
This does not license guessing: a code you do not recognise is judged on its
display like any other candidate, and a display that is vaguer than the concept is
still a reason to reject a code you have no independent knowledge of.

For a drug class, select the entries whose ingredient belongs to that class.
Different strengths or formulations of the same ingredient are all matches.

A candidate can also be BROADER than the concept: it contains what the criterion
asks for and other things as well. A site that records every diabetes diagnosis
under one unspecified diabetes code has a code that is broader than "type 2
diabetes mellitus". A site whose only asthma code is "chronic lower respiratory
disease" has one that is broader than asthma.

Put those in `broader_codes`, not in `codes`. The difference decides what happens
to a patient. A code in `codes` proves the concept when it is present. A code in
`broader_codes` cannot prove it, and the criterion comes back undetermined for
that patient rather than satisfied. Its absence still counts: a patient with no
diabetes code of any kind does not have type 2 diabetes either.

Do not use `broader_codes` for a code that is merely related, or for a
complication of the concept, or for a different thing that shares a word. Only
for a code whose meaning genuinely contains the concept.

If no candidate represents the concept and none contains it, return both lists
empty. That is a normal and useful answer.

Return JSON only:
{"codes": ["4548-4"], "broader_codes": [],
  "rejected": [{"code": "718-7", "why": "different analyte"}],
  "confidence": "high"}
```

### 33. llm_response (openai, 42 tok, 28.220448970794678s)

```
```json
{
  "codes": [
    "312617",
    "312615",
    "106258",
    "895994",
    "896209"
  ],
  "broader_codes": [],
  "rejected": [],
  "confidence": "high"
}
```
```

### 34. final

```json
{
 "broader_codes": [],
 "codes": [
  "106258",
  "312615",
  "312617",
  "895994",
  "896209"
 ],
 "concept": "Glucocorticoid",
 "confidence": "high",
 "displays": [
  "predniSONE 5 MG Oral Tablet",
  "predniSONE 20 MG Oral Tablet",
  "Hydrocortisone 10 MG/ML Topical Cream",
  "120 ACTUAT Fluticasone propionate 0.044 MG/ACTUAT Metered Dose Inhaler",
  "60 ACTUAT Fluticasone propionate 0.25 MG/ACTUAT / salmeterol 0.05 MG/ACTUAT Dry Powder Inhaler"
 ],
 "domain": "medication",
 "expanded_names": [
  "prednisone",
  "prednisolone",
  "methylprednisolone",
  "dexamethasone",
  "hydrocortisone",
  "triamcinolone",
  "betamethasone",
  "budesonide",
  "fluticasone",
  "beclomethasone",
  "mometasone",
  "cortisone",
  "deflazacort",
  "clobetasol"
 ],
 "matched_names": [
  "cortisone",
  "fluticasone",
  "hydrocortisone",
  "prednisone"
 ],
 "reason": "5 of 5 candidates selected",
 "status": "MAPPED",
 "unmatched_names": [
  "beclomethasone",
  "betamethasone",
  "budesonide",
  "clobetasol",
  "deflazacort",
  "dexamethasone",
  "methylprednisolone",
  "mometasone",
  "prednisolone",
  "triamcinolone"
 ]
}
```

### 35. instructions (prompt grounder-v3)

```
You are a clinical terminologist. You expand a clinical concept into the
specific named things a medical record would actually store for it.

Expand this clinical concept into concrete searchable names.

Concept: {concept}
Record domain: {domain}

For a drug class, list the generic ingredient names that belong to the class.
For a diagnosis, list the specific condition names, including the common
sub-types a record might code instead of the general term.
For a laboratory test, list the names and common synonyms of the measurement.

List what belongs to the concept in general clinical practice. Do NOT try to
guess what this particular site happens to record; that is looked up separately.

Return JSON only:
{{"names": ["empagliflozin", "dapagliflozin"], "note": "one short line"}}

Give between 1 and 15 names.

---

You match a clinical concept against a specific site's coded vocabulary.

You choose only from the candidates given. You never invent a code. If none of
the candidates genuinely represents the concept, you say so plainly, because a
wrong match here is worse than no match.

Concept: {concept}
Record domain: {domain}
Meaning required: {intent}

Candidate entries from this site's vocabulary:
{candidates}

Select every candidate that genuinely represents the concept. Judge by what the
entry means, not by whether the words look similar. A different measurement that
shares a word is not a match: "Respiratory rate" is not a glomerular filtration
rate, and "Chronic sinusitis" is not chronic kidney disease.

The display text beside each code is whatever the source system chose to store.
It is frequently abbreviated, locally worded, or vaguer than the code it labels,
and it is not the code's definition. Judge the concept the code denotes. Where you
recognise a code and its display disagree about how specific the concept is, the
code decides, and name the code you relied on in `why` for the ones you rejected.
This does not license guessing: a code you do not recognise is judged on its
display like any other candidate, and a display that is vaguer than the concept is
still a reason to reject a code you have no independent knowledge of.

For a drug class, select the entries whose ingredient belongs to that class.
Different strengths or formulations of the same ingredient are all matches.

A candidate can also be BROADER than the concept: it contains what the criterion
asks for and other things as well. A site that records every diabetes diagnosis
under one unspecified diabetes code has a code that is broader than "type 2
diabetes mellitus". A site whose only asthma code is "chronic lower respiratory
disease" has one that is broader than asthma.

Put those in `broader_codes`, not in `codes`. The difference decides what happens
to a patient. A code in `codes` proves the concept when it is present. A code in
`broader_codes` cannot prove it, and the criterion comes back undetermined for
that patient rather than satisfied. Its absence still counts: a patient with no
diabetes code of any kind does not have type 2 diabetes either.

Do not use `broader_codes` for a code that is merely related, or for a
complication of the concept, or for a different thing that shares a word. Only
for a code whose meaning genuinely contains the concept.

If no candidate represents the concept and none contains it, return both lists
empty. That is a normal and useful answer.

Return JSON only:
{{"codes": ["4548-4"], "broader_codes": [],
  "rejected": [{{"code": "718-7", "why": "different analyte"}}],
  "confidence": "high"}}
```

### 36. input

```json
{
 "concept": "Immunosuppressant",
 "domain": "medication",
 "intent": "currently prescribed or active drug of this class"
}
```

### 37. llm_request -> gemini-3.7-flash-medium  cassette `c33071bf1c831a13`

```
[system]
You are a clinical terminologist. You expand a clinical concept into the
specific named things a medical record would actually store for it.

[user]
Expand this clinical concept into concrete searchable names.

Concept: Immunosuppressant
Record domain: medication

For a drug class, list the generic ingredient names that belong to the class.
For a diagnosis, list the specific condition names, including the common
sub-types a record might code instead of the general term.
For a laboratory test, list the names and common synonyms of the measurement.

List what belongs to the concept in general clinical practice. Do NOT try to
guess what this particular site happens to record; that is looked up separately.

Return JSON only:
{"names": ["empagliflozin", "dapagliflozin"], "note": "one short line"}

Give between 1 and 15 names.
```

### 38. llm_response (openai, 113 tok, 28.45481824874878s)

```
```json
{
  "names": [
    "tacrolimus",
    "cyclosporine",
    "mycophenolate mofetil",
    "azathioprine",
    "methotrexate",
    "prednisone",
    "sirolimus",
    "everolimus",
    "belatacept",
    "cyclophosphamide",
    "leflunomide",
    "rituximab",
    "infliximab",
    "adalimumab"
  ],
  "note": "Common systemic immunosuppressive agents spanning calcineurin inhibitors, antimetabolites, mTOR inhibitors, steroids, and biologics."
}
```
```

### 39. tool_call `terminology.search_any`

```json
{
 "domain": "medication",
 "limit": 12,
 "terms": [
  "tacrolimus",
  "cyclosporine",
  "mycophenolate mofetil",
  "azathioprine",
  "methotrexate",
  "prednisone",
  "sirolimus",
  "everolimus",
  "belatacept",
  "cyclophosphamide",
  "leflunomide",
  "rituximab",
  "infliximab",
  "adalimumab"
 ]
}
```

### 40. tool_result `terminology.search_any`

```json
[
 {
  "code": "1734919",
  "display": "Cyclophosphamide 1000 MG Injection",
  "domain": "medication",
  "n_resources": 16
 },
 {
  "code": "312617",
  "display": "predniSONE 5 MG Oral Tablet",
  "domain": "medication",
  "n_resources": 6
 },
 {
  "code": "312615",
  "display": "predniSONE 20 MG Oral Tablet",
  "domain": "medication",
  "n_resources": 1
 },
 {
  "code": "105585",
  "display": "Methotrexate 2.5 MG Oral Tablet",
  "domain": "medication",
  "n_resources": 1
 }
]
```

### 41. llm_request -> gemini-3.7-flash-medium  cassette `3adbb7cebd432a02`

```
[system]
You match a clinical concept against a specific site's coded vocabulary.

You choose only from the candidates given. You never invent a code. If none of
the candidates genuinely represents the concept, you say so plainly, because a
wrong match here is worse than no match.

[user]
Concept: Immunosuppressant
Record domain: medication
Meaning required: currently prescribed or active drug of this class

Candidate entries from this site's vocabulary:
           1734919  Cyclophosphamide 1000 MG Injection   (in 16 resources)
            312617  predniSONE 5 MG Oral Tablet   (in 6 resources)
            312615  predniSONE 20 MG Oral Tablet   (in 1 resources)
            105585  Methotrexate 2.5 MG Oral Tablet   (in 1 resources)

Select every candidate that genuinely represents the concept. Judge by what the
entry means, not by whether the words look similar. A different measurement that
shares a word is not a match: "Respiratory rate" is not a glomerular filtration
rate, and "Chronic sinusitis" is not chronic kidney disease.

The display text beside each code is whatever the source system chose to store.
It is frequently abbreviated, locally worded, or vaguer than the code it labels,
and it is not the code's definition. Judge the concept the code denotes. Where you
recognise a code and its display disagree about how specific the concept is, the
code decides, and name the code you relied on in `why` for the ones you rejected.
This does not license guessing: a code you do not recognise is judged on its
display like any other candidate, and a display that is vaguer than the concept is
still a reason to reject a code you have no independent knowledge of.

For a drug class, select the entries whose ingredient belongs to that class.
Different strengths or formulations of the same ingredient are all matches.

A candidate can also be BROADER than the concept: it contains what the criterion
asks for and other things as well. A site that records every diabetes diagnosis
under one unspecified diabetes code has a code that is broader than "type 2
diabetes mellitus". A site whose only asthma code is "chronic lower respiratory
disease" has one that is broader than asthma.

Put those in `broader_codes`, not in `codes`. The difference decides what happens
to a patient. A code in `codes` proves the concept when it is present. A code in
`broader_codes` cannot prove it, and the criterion comes back undetermined for
that patient rather than satisfied. Its absence still counts: a patient with no
diabetes code of any kind does not have type 2 diabetes either.

Do not use `broader_codes` for a code that is merely related, or for a
complication of the concept, or for a different thing that shares a word. Only
for a code whose meaning genuinely contains the concept.

If no candidate represents the concept and none contains it, return both lists
empty. That is a normal and useful answer.

Return JSON only:
{"codes": ["4548-4"], "broader_codes": [],
  "rejected": [{"code": "718-7", "why": "different analyte"}],
  "confidence": "high"}
```

### 42. llm_response (openai, 39 tok, 30.60789680480957s)

```
```json
{
  "codes": [
    "1734919",
    "312617",
    "312615",
    "105585"
  ],
  "broader_codes": [],
  "rejected": [],
  "confidence": "high"
}
```
```

### 43. final

```json
{
 "broader_codes": [],
 "codes": [
  "105585",
  "1734919",
  "312615",
  "312617"
 ],
 "concept": "Immunosuppressant",
 "confidence": "high",
 "displays": [
  "Cyclophosphamide 1000 MG Injection",
  "predniSONE 5 MG Oral Tablet",
  "predniSONE 20 MG Oral Tablet",
  "Methotrexate 2.5 MG Oral Tablet"
 ],
 "domain": "medication",
 "expanded_names": [
  "tacrolimus",
  "cyclosporine",
  "mycophenolate mofetil",
  "azathioprine",
  "methotrexate",
  "prednisone",
  "sirolimus",
  "everolimus",
  "belatacept",
  "cyclophosphamide",
  "leflunomide",
  "rituximab",
  "infliximab",
  "adalimumab"
 ],
 "matched_names": [
  "cyclophosphamide",
  "methotrexate",
  "prednisone"
 ],
 "reason": "4 of 4 candidates selected",
 "status": "PARTIAL",
 "unmatched_names": [
  "adalimumab",
  "azathioprine",
  "belatacept",
  "cyclosporine",
  "everolimus",
  "infliximab",
  "leflunomide",
  "mycophenolate mofetil",
  "rituximab",
  "sirolimus",
  "tacrolimus"
 ]
}
```

### 44. instructions (prompt grounder-v3)

```
You are a clinical terminologist. You expand a clinical concept into the
specific named things a medical record would actually store for it.

Expand this clinical concept into concrete searchable names.

Concept: {concept}
Record domain: {domain}

For a drug class, list the generic ingredient names that belong to the class.
For a diagnosis, list the specific condition names, including the common
sub-types a record might code instead of the general term.
For a laboratory test, list the names and common synonyms of the measurement.

List what belongs to the concept in general clinical practice. Do NOT try to
guess what this particular site happens to record; that is looked up separately.

Return JSON only:
{{"names": ["empagliflozin", "dapagliflozin"], "note": "one short line"}}

Give between 1 and 15 names.

---

You match a clinical concept against a specific site's coded vocabulary.

You choose only from the candidates given. You never invent a code. If none of
the candidates genuinely represents the concept, you say so plainly, because a
wrong match here is worse than no match.

Concept: {concept}
Record domain: {domain}
Meaning required: {intent}

Candidate entries from this site's vocabulary:
{candidates}

Select every candidate that genuinely represents the concept. Judge by what the
entry means, not by whether the words look similar. A different measurement that
shares a word is not a match: "Respiratory rate" is not a glomerular filtration
rate, and "Chronic sinusitis" is not chronic kidney disease.

The display text beside each code is whatever the source system chose to store.
It is frequently abbreviated, locally worded, or vaguer than the code it labels,
and it is not the code's definition. Judge the concept the code denotes. Where you
recognise a code and its display disagree about how specific the concept is, the
code decides, and name the code you relied on in `why` for the ones you rejected.
This does not license guessing: a code you do not recognise is judged on its
display like any other candidate, and a display that is vaguer than the concept is
still a reason to reject a code you have no independent knowledge of.

For a drug class, select the entries whose ingredient belongs to that class.
Different strengths or formulations of the same ingredient are all matches.

A candidate can also be BROADER than the concept: it contains what the criterion
asks for and other things as well. A site that records every diabetes diagnosis
under one unspecified diabetes code has a code that is broader than "type 2
diabetes mellitus". A site whose only asthma code is "chronic lower respiratory
disease" has one that is broader than asthma.

Put those in `broader_codes`, not in `codes`. The difference decides what happens
to a patient. A code in `codes` proves the concept when it is present. A code in
`broader_codes` cannot prove it, and the criterion comes back undetermined for
that patient rather than satisfied. Its absence still counts: a patient with no
diabetes code of any kind does not have type 2 diabetes either.

Do not use `broader_codes` for a code that is merely related, or for a
complication of the concept, or for a different thing that shares a word. Only
for a code whose meaning genuinely contains the concept.

If no candidate represents the concept and none contains it, return both lists
empty. That is a normal and useful answer.

Return JSON only:
{{"codes": ["4548-4"], "broader_codes": [],
  "rejected": [{{"code": "718-7", "why": "different analyte"}}],
  "confidence": "high"}}
```

### 45. input

```json
{
 "concept": "Antimicrobial agent",
 "domain": "medication",
 "intent": "currently prescribed or active drug of this class"
}
```

### 46. llm_request -> gemini-3.7-flash-medium  cassette `8c8cd2623f08f18d`

```
[system]
You are a clinical terminologist. You expand a clinical concept into the
specific named things a medical record would actually store for it.

[user]
Expand this clinical concept into concrete searchable names.

Concept: Antimicrobial agent
Record domain: medication

For a drug class, list the generic ingredient names that belong to the class.
For a diagnosis, list the specific condition names, including the common
sub-types a record might code instead of the general term.
For a laboratory test, list the names and common synonyms of the measurement.

List what belongs to the concept in general clinical practice. Do NOT try to
guess what this particular site happens to record; that is looked up separately.

Return JSON only:
{"names": ["empagliflozin", "dapagliflozin"], "note": "one short line"}

Give between 1 and 15 names.
```

### 47. llm_response (openai, 108 tok, 32.808093309402466s)

```
{
  "names": [
    "amoxicillin",
    "ciprofloxacin",
    "azithromycin",
    "ceftriaxone",
    "doxycycline",
    "vancomycin",
    "metronidazole",
    "levofloxacin",
    "trimethoprim",
    "sulfamethoxazole",
    "fluconazole",
    "acyclovir",
    "piperacillin",
    "meropenem",
    "cephalexin"
  ],
  "note": "Common generic antimicrobial ingredients spanning major antibacterial, antifungal, and antiviral classes."
}
```

### 48. tool_call `terminology.search_any`

```json
{
 "domain": "medication",
 "limit": 12,
 "terms": [
  "amoxicillin",
  "ciprofloxacin",
  "azithromycin",
  "ceftriaxone",
  "doxycycline",
  "vancomycin",
  "metronidazole",
  "levofloxacin",
  "trimethoprim",
  "sulfamethoxazole",
  "fluconazole",
  "acyclovir",
  "piperacillin",
  "meropenem",
  "cephalexin"
 ]
}
```

### 49. tool_result `terminology.search_any`

```json
[
 {
  "code": "308192",
  "display": "Amoxicillin 500 MG Oral Tablet",
  "domain": "medication",
  "n_resources": 60
 },
 {
  "code": "212446",
  "display": "Azithromycin 250mg",
  "domain": "medication",
  "n_resources": 2
 },
 {
  "code": "308182",
  "display": "Amoxicillin 250 MG Oral Capsule",
  "domain": "medication",
  "n_resources": 16
 },
 {
  "code": "1649987",
  "display": "doxycycline hyclate 100 MG",
  "domain": "medication",
  "n_resources": 6
 },
 {
  "code": "562251",
  "display": "Amoxicillin 250 MG / Clavulanate 125 MG Oral Tablet",
  "domain": "medication",
  "n_resources": 125
 },
 {
  "code": "1659149",
  "display": "Piperacillin 4000 MG / tazobactam 500 MG Injection",
  "domain": "medication",
  "n_resources": 2
 }
]
```

### 50. llm_request -> gemini-3.7-flash-medium  cassette `55c6c5cf3d979761`

```
[system]
You match a clinical concept against a specific site's coded vocabulary.

You choose only from the candidates given. You never invent a code. If none of
the candidates genuinely represents the concept, you say so plainly, because a
wrong match here is worse than no match.

[user]
Concept: Antimicrobial agent
Record domain: medication
Meaning required: currently prescribed or active drug of this class

Candidate entries from this site's vocabulary:
            308192  Amoxicillin 500 MG Oral Tablet   (in 60 resources)
            212446  Azithromycin 250mg   (in 2 resources)
            308182  Amoxicillin 250 MG Oral Capsule   (in 16 resources)
           1649987  doxycycline hyclate 100 MG   (in 6 resources)
            562251  Amoxicillin 250 MG / Clavulanate 125 MG Oral Tablet   (in 125 resources)
           1659149  Piperacillin 4000 MG / tazobactam 500 MG Injection   (in 2 resources)

Select every candidate that genuinely represents the concept. Judge by what the
entry means, not by whether the words look similar. A different measurement that
shares a word is not a match: "Respiratory rate" is not a glomerular filtration
rate, and "Chronic sinusitis" is not chronic kidney disease.

The display text beside each code is whatever the source system chose to store.
It is frequently abbreviated, locally worded, or vaguer than the code it labels,
and it is not the code's definition. Judge the concept the code denotes. Where you
recognise a code and its display disagree about how specific the concept is, the
code decides, and name the code you relied on in `why` for the ones you rejected.
This does not license guessing: a code you do not recognise is judged on its
display like any other candidate, and a display that is vaguer than the concept is
still a reason to reject a code you have no independent knowledge of.

For a drug class, select the entries whose ingredient belongs to that class.
Different strengths or formulations of the same ingredient are all matches.

A candidate can also be BROADER than the concept: it contains what the criterion
asks for and other things as well. A site that records every diabetes diagnosis
under one unspecified diabetes code has a code that is broader than "type 2
diabetes mellitus". A site whose only asthma code is "chronic lower respiratory
disease" has one that is broader than asthma.

Put those in `broader_codes`, not in `codes`. The difference decides what happens
to a patient. A code in `codes` proves the concept when it is present. A code in
`broader_codes` cannot prove it, and the criterion comes back undetermined for
that patient rather than satisfied. Its absence still counts: a patient with no
diabetes code of any kind does not have type 2 diabetes either.

Do not use `broader_codes` for a code that is merely related, or for a
complication of the concept, or for a different thing that shares a word. Only
for a code whose meaning genuinely contains the concept.

If no candidate represents the concept and none contains it, return both lists
empty. That is a normal and useful answer.

Return JSON only:
{"codes": ["4548-4"], "broader_codes": [],
  "rejected": [{"code": "718-7", "why": "different analyte"}],
  "confidence": "high"}
```

### 51. llm_response (openai, 46 tok, 33.15716314315796s)

```
```json
{
  "codes": [
    "308192",
    "212446",
    "308182",
    "1649987",
    "562251",
    "1659149"
  ],
  "broader_codes": [],
  "rejected": [],
  "confidence": "high"
}
```
```

### 52. final

```json
{
 "broader_codes": [],
 "codes": [
  "1649987",
  "1659149",
  "212446",
  "308182",
  "308192",
  "562251"
 ],
 "concept": "Antimicrobial agent",
 "confidence": "high",
 "displays": [
  "Amoxicillin 500 MG Oral Tablet",
  "Azithromycin 250mg",
  "Amoxicillin 250 MG Oral Capsule",
  "doxycycline hyclate 100 MG",
  "Amoxicillin 250 MG / Clavulanate 125 MG Oral Tablet",
  "Piperacillin 4000 MG / tazobactam 500 MG Injection"
 ],
 "domain": "medication",
 "expanded_names": [
  "amoxicillin",
  "ciprofloxacin",
  "azithromycin",
  "ceftriaxone",
  "doxycycline",
  "vancomycin",
  "metronidazole",
  "levofloxacin",
  "trimethoprim",
  "sulfamethoxazole",
  "fluconazole",
  "acyclovir",
  "piperacillin",
  "meropenem",
  "cephalexin"
 ],
 "matched_names": [
  "amoxicillin",
  "azithromycin",
  "doxycycline",
  "piperacillin"
 ],
 "reason": "6 of 6 candidates selected",
 "status": "PARTIAL",
 "unmatched_names": [
  "acyclovir",
  "ceftriaxone",
  "cephalexin",
  "ciprofloxacin",
  "fluconazole",
  "levofloxacin",
  "meropenem",
  "metronidazole",
  "sulfamethoxazole",
  "trimethoprim",
  "vancomycin"
 ]
}
```

### 53. instructions (prompt grounder-v3)

```
You are a clinical terminologist. You expand a clinical concept into the
specific named things a medical record would actually store for it.

Expand this clinical concept into concrete searchable names.

Concept: {concept}
Record domain: {domain}

For a drug class, list the generic ingredient names that belong to the class.
For a diagnosis, list the specific condition names, including the common
sub-types a record might code instead of the general term.
For a laboratory test, list the names and common synonyms of the measurement.

List what belongs to the concept in general clinical practice. Do NOT try to
guess what this particular site happens to record; that is looked up separately.

Return JSON only:
{{"names": ["empagliflozin", "dapagliflozin"], "note": "one short line"}}

Give between 1 and 15 names.

---

You match a clinical concept against a specific site's coded vocabulary.

You choose only from the candidates given. You never invent a code. If none of
the candidates genuinely represents the concept, you say so plainly, because a
wrong match here is worse than no match.

Concept: {concept}
Record domain: {domain}
Meaning required: {intent}

Candidate entries from this site's vocabulary:
{candidates}

Select every candidate that genuinely represents the concept. Judge by what the
entry means, not by whether the words look similar. A different measurement that
shares a word is not a match: "Respiratory rate" is not a glomerular filtration
rate, and "Chronic sinusitis" is not chronic kidney disease.

The display text beside each code is whatever the source system chose to store.
It is frequently abbreviated, locally worded, or vaguer than the code it labels,
and it is not the code's definition. Judge the concept the code denotes. Where you
recognise a code and its display disagree about how specific the concept is, the
code decides, and name the code you relied on in `why` for the ones you rejected.
This does not license guessing: a code you do not recognise is judged on its
display like any other candidate, and a display that is vaguer than the concept is
still a reason to reject a code you have no independent knowledge of.

For a drug class, select the entries whose ingredient belongs to that class.
Different strengths or formulations of the same ingredient are all matches.

A candidate can also be BROADER than the concept: it contains what the criterion
asks for and other things as well. A site that records every diabetes diagnosis
under one unspecified diabetes code has a code that is broader than "type 2
diabetes mellitus". A site whose only asthma code is "chronic lower respiratory
disease" has one that is broader than asthma.

Put those in `broader_codes`, not in `codes`. The difference decides what happens
to a patient. A code in `codes` proves the concept when it is present. A code in
`broader_codes` cannot prove it, and the criterion comes back undetermined for
that patient rather than satisfied. Its absence still counts: a patient with no
diabetes code of any kind does not have type 2 diabetes either.

Do not use `broader_codes` for a code that is merely related, or for a
complication of the concept, or for a different thing that shares a word. Only
for a code whose meaning genuinely contains the concept.

If no candidate represents the concept and none contains it, return both lists
empty. That is a normal and useful answer.

Return JSON only:
{{"codes": ["4548-4"], "broader_codes": [],
  "rejected": [{{"code": "718-7", "why": "different analyte"}}],
  "confidence": "high"}}
```

### 54. input

```json
{
 "concept": "Chemotherapeutic agent",
 "domain": "medication",
 "intent": "currently prescribed or active drug of this class"
}
```

### 55. llm_request -> gemini-3.7-flash-medium  cassette `8dcc27fa4d7f191e`

```
[system]
You are a clinical terminologist. You expand a clinical concept into the
specific named things a medical record would actually store for it.

[user]
Expand this clinical concept into concrete searchable names.

Concept: Chemotherapeutic agent
Record domain: medication

For a drug class, list the generic ingredient names that belong to the class.
For a diagnosis, list the specific condition names, including the common
sub-types a record might code instead of the general term.
For a laboratory test, list the names and common synonyms of the measurement.

List what belongs to the concept in general clinical practice. Do NOT try to
guess what this particular site happens to record; that is looked up separately.

Return JSON only:
{"names": ["empagliflozin", "dapagliflozin"], "note": "one short line"}

Give between 1 and 15 names.
```

### 56. llm_response (openai, 100 tok, 28.74376916885376s)

```
{
  "names": [
    "cisplatin",
    "carboplatin",
    "oxaliplatin",
    "fluorouracil",
    "capecitabine",
    "gemcitabine",
    "methotrexate",
    "pemetrexed",
    "doxorubicin",
    "paclitaxel",
    "docetaxel",
    "cyclophosphamide",
    "irinotecan",
    "etoposide",
    "vincristine"
  ],
  "note": "Common cytotoxic and antineoplastic chemotherapy agents across major drug classes."
}
```

### 57. tool_call `terminology.search_any`

```json
{
 "domain": "medication",
 "limit": 12,
 "terms": [
  "cisplatin",
  "carboplatin",
  "oxaliplatin",
  "fluorouracil",
  "capecitabine",
  "gemcitabine",
  "methotrexate",
  "pemetrexed",
  "doxorubicin",
  "paclitaxel",
  "docetaxel",
  "cyclophosphamide",
  "irinotecan",
  "etoposide",
  "vincristine"
 ]
}
```

### 58. tool_result `terminology.search_any`

```json
[
 {
  "code": "583214",
  "display": "PACLitaxel 100 MG Injection",
  "domain": "medication",
  "n_resources": 90
 },
 {
  "code": "1736854",
  "display": "Cisplatin 50 MG Injection",
  "domain": "medication",
  "n_resources": 66
 },
 {
  "code": "1734919",
  "display": "Cyclophosphamide 1000 MG Injection",
  "domain": "medication",
  "n_resources": 16
 },
 {
  "code": "1736776",
  "display": "10 ML oxaliplatin 5 MG/ML Injection",
  "domain": "medication",
  "n_resources": 7
 },
 {
  "code": "1860480",
  "display": "1 ML DOCEtaxel 20 MG/ML Injection",
  "domain": "medication",
  "n_resources": 7
 },
 {
  "code": "105585",
  "display": "Methotrexate 2.5 MG Oral Tablet",
  "domain": "medication",
  "n_resources": 1
 },
 {
  "code": "597195",
  "display": "Carboplatin 10 MG/ML Injectable Solution",
  "domain": "medication",
  "n_resources": 8
 }
]
```

### 59. llm_request -> gemini-3.7-flash-medium  cassette `30956ede9fb0ec52`

```
[system]
You match a clinical concept against a specific site's coded vocabulary.

You choose only from the candidates given. You never invent a code. If none of
the candidates genuinely represents the concept, you say so plainly, because a
wrong match here is worse than no match.

[user]
Concept: Chemotherapeutic agent
Record domain: medication
Meaning required: currently prescribed or active drug of this class

Candidate entries from this site's vocabulary:
            583214  PACLitaxel 100 MG Injection   (in 90 resources)
           1736854  Cisplatin 50 MG Injection   (in 66 resources)
           1734919  Cyclophosphamide 1000 MG Injection   (in 16 resources)
           1736776  10 ML oxaliplatin 5 MG/ML Injection   (in 7 resources)
           1860480  1 ML DOCEtaxel 20 MG/ML Injection   (in 7 resources)
            105585  Methotrexate 2.5 MG Oral Tablet   (in 1 resources)
            597195  Carboplatin 10 MG/ML Injectable Solution   (in 8 resources)

Select every candidate that genuinely represents the concept. Judge by what the
entry means, not by whether the words look similar. A different measurement that
shares a word is not a match: "Respiratory rate" is not a glomerular filtration
rate, and "Chronic sinusitis" is not chronic kidney disease.

The display text beside each code is whatever the source system chose to store.
It is frequently abbreviated, locally worded, or vaguer than the code it labels,
and it is not the code's definition. Judge the concept the code denotes. Where you
recognise a code and its display disagree about how specific the concept is, the
code decides, and name the code you relied on in `why` for the ones you rejected.
This does not license guessing: a code you do not recognise is judged on its
display like any other candidate, and a display that is vaguer than the concept is
still a reason to reject a code you have no independent knowledge of.

For a drug class, select the entries whose ingredient belongs to that class.
Different strengths or formulations of the same ingredient are all matches.

A candidate can also be BROADER than the concept: it contains what the criterion
asks for and other things as well. A site that records every diabetes diagnosis
under one unspecified diabetes code has a code that is broader than "type 2
diabetes mellitus". A site whose only asthma code is "chronic lower respiratory
disease" has one that is broader than asthma.

Put those in `broader_codes`, not in `codes`. The difference decides what happens
to a patient. A code in `codes` proves the concept when it is present. A code in
`broader_codes` cannot prove it, and the criterion comes back undetermined for
that patient rather than satisfied. Its absence still counts: a patient with no
diabetes code of any kind does not have type 2 diabetes either.

Do not use `broader_codes` for a code that is merely related, or for a
complication of the concept, or for a different thing that shares a word. Only
for a code whose meaning genuinely contains the concept.

If no candidate represents the concept and none contains it, return both lists
empty. That is a normal and useful answer.

Return JSON only:
{"codes": ["4548-4"], "broader_codes": [],
  "rejected": [{"code": "718-7", "why": "different analyte"}],
  "confidence": "high"}
```

### 60. llm_response (openai, 50 tok, 34.457059383392334s)

```
```json
{
  "codes": [
    "583214",
    "1736854",
    "1734919",
    "1736776",
    "1860480",
    "105585",
    "597195"
  ],
  "broader_codes": [],
  "rejected": [],
  "confidence": "high"
}
```
```

### 61. final

```json
{
 "broader_codes": [],
 "codes": [
  "105585",
  "1734919",
  "1736776",
  "1736854",
  "1860480",
  "583214",
  "597195"
 ],
 "concept": "Chemotherapeutic agent",
 "confidence": "high",
 "displays": [
  "PACLitaxel 100 MG Injection",
  "Cisplatin 50 MG Injection",
  "Cyclophosphamide 1000 MG Injection",
  "10 ML oxaliplatin 5 MG/ML Injection",
  "1 ML DOCEtaxel 20 MG/ML Injection",
  "Methotrexate 2.5 MG Oral Tablet",
  "Carboplatin 10 MG/ML Injectable Solution"
 ],
 "domain": "medication",
 "expanded_names": [
  "cisplatin",
  "carboplatin",
  "oxaliplatin",
  "fluorouracil",
  "capecitabine",
  "gemcitabine",
  "methotrexate",
  "pemetrexed",
  "doxorubicin",
  "paclitaxel",
  "docetaxel",
  "cyclophosphamide",
  "irinotecan",
  "etoposide",
  "vincristine"
 ],
 "matched_names": [
  "carboplatin",
  "cisplatin",
  "cyclophosphamide",
  "docetaxel",
  "methotrexate",
  "oxaliplatin",
  "paclitaxel"
 ],
 "reason": "7 of 7 candidates selected",
 "status": "MAPPED",
 "unmatched_names": [
  "capecitabine",
  "doxorubicin",
  "etoposide",
  "fluorouracil",
  "gemcitabine",
  "irinotecan",
  "pemetrexed",
  "vincristine"
 ]
}
```

### 62. final

```json
{
 "compilable": false,
 "reason_not_compilable": "cannot be represented in this site's vocabulary. SGLT2 inhibitor: no entry in this site's medication vocabulary matches any of empagliflozin, dapagliflozin, canagliflozin, ertugliflozin, bexagliflozin, sotagliflozin and others; Thiazolidinedione (TZD): no entry in this site's medication vocabulary matches any of pioglitazone, rosiglitazone, troglitazone, lobeglitazone, balaglitazone, ciglitazone and others; GLP-1 receptor agonist (GLP-1RA): no entry in this site's medication vocabulary matches any of semaglutide, liraglutide, dulaglutide, exenatide, lixisenatide, tirzepatide and others"
}
```

